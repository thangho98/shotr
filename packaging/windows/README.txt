shotr — screenshot beautifier

Run shotr.exe. Nothing else to install.

Text recognition (OCR):
  Vietnamese needs Tesseract with the `vie` language pack.
  Get it from https://github.com/UB-Mannheim/tesseract/wiki
  and add the install folder to PATH.
  Without Tesseract, shotr still reads unaccented Latin text.
